'use client';

import { HomeSection, ProjectSection, SkillSection } from '@/components/layout';

export default function Homepage() {
  return (
    <main>
      <HomeSection />
      <SkillSection />
      <ProjectSection />
    </main>
  );
}
